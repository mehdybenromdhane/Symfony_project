

<h1><a href="{{path('add')}}"> add author</a></h1>

<table border="1" align="center">

    <caption>List of authors</caption>

    <tr>

        <td>Id</td>
        <td>Username</td>
        <td>Email</td>
        <td>Update</td>
        <td>Delete</td>

    </tr>



    {% for a in list %}
    <tr>
        <td>{{a.id}}</td>
        <td>{{a.username}}</td>
        <td>{{a.email}}</td>
        <td><a href="{{path('update' , {'id':a.id})}}"> update</a></td>
   <td><a href="{{path('delete',{'id':a.id})}}"> delete</a></td>
   
    </tr>

    {% endfor %}
</table>